export interface Tile {
    id: string;
    img: string;
    title: string;
}

export interface Trigger {
    title: string;
    description: string;
}

export interface Consequence {
    title: string;
    description: string;
}
